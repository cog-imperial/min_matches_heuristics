

# The classes of the objects represent solutions of either the transhipment or the transportation MIP.
class Mip_Solution:

	def __init__(self,*args):
		
		self.model=args[0]
		self.n = args[1] # number of heaters
		self.m = args[2] # number of coolers
		self.k = args[3] # number of temperature intervals
		print('paparia')
		if self.model=='transhipment':
			print('paparia')
			self.y = agrs[4] # indicator vector of matches
			self.q = args[5]
			self.r = args[6]
		
		if self.model=='transportation':
			self.y = args[4] # indicator vector of matches
			self.q = args[5] # vector
